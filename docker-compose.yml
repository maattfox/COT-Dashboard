version: '3.3'

services:
   db:
     container_name: mysql
     image: mysql:5.7
     volumes:
       - "~/data:/var/lib/mysql"
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: root

